{
  /* The main  purpose of this file  is to receive the data from our server which is from our 
node server just to link our node server to our react project e.g: face mask app in this case 
This is the class based approach to send / receive data from server */
}
/*
import axios from "axios";

let url = "http://localhost:4000";
export function getProducts() {
  return axios.get(`${url}/products`).then((response) => response.data);
}
*/
