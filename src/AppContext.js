/**
 *Since we are using a class based component we are putting  reactAppContext in its own component
 */

import React from "react";

let AppContext = React.createContext();

export default AppContext;
